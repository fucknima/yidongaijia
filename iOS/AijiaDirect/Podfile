platform :ios, '15.0'

target 'AijiaDirect' do
  use_frameworks!
  # Local ijkplayer fork built by .github/workflows/build-ijk.yml. The
  # framework is downloaded from the ijk-0.8.8-record release before
  # pod install in the main build workflow.
  pod 'IJKMediaFramework', :path => 'IJKMediaFramework'
end
